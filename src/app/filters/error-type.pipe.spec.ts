import { ErrorTypePipe } from './error-type.pipe';

describe('ErrorTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ErrorTypePipe();
    expect(pipe).toBeTruthy();
  });
});
